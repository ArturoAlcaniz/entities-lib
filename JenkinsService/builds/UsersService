pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                script{
                    sh '''
                    ls && 
                    cd UsersService && 
                    su &&
                    bash docker_build.bash
                    '''
                }
            }
        }
    }
}