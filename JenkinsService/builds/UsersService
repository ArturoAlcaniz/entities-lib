pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                script{
                    sh '''
                    ls && 
                    cd UsersService && 
                    newgrp - docker &&
                    bash docker_build.bash
                    '''
                }
            }
        }
    }
}